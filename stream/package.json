{
  "name": "stream",
  "version": "1.0.0",
  "description": "Continuously streams approved tracks to a YouTube Live RTMP endpoint. Artists submit tracks, admins approve them, and this pipeline builds a playlist and pushes it as a looping live stream.",
  "main": "auto-refresh.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "dotenv": "^17.3.1"
  }
}
